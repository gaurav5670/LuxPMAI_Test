The packages required can be installed through requirements.txt file.
This project shows how to create a simple 3-Layer classification model for classify two class of fruits.
It uses Keras to train the model and sklearn to calculate metrices.
OpenCV is used to read and resize image files to desired size.
Train and validation set is created as a numpy array with there labels.